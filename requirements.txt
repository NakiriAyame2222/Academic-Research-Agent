# 直接依赖：config.py / tools/arxiv_search.py 直接 import
openai>=1.40.0
requests>=2.32.0

langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
pyyaml>=6.0.1
pypdf>=5.0.0
pymupdf>=1.24.9
fastmcp>=0.1.0

# 检索增强：均为可选，未安装时代码自动降级
chromadb>=0.5.0
jieba>=0.42.1

# Web 适配层（api/）：FastAPI + SSE。装好后 tests/test_api.py 才会运行，否则自动 skip。
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
python-multipart>=0.0.9      # 文件上传（/api/papers/upload）必需
